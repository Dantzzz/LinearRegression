using System;
using System.Collections.Generic;
using System.Text;
using System.Threading.Tasks;

namespace LinearRegression
{
    public class Regression
    {
        public static void CalculateValues()
        {
            
            //TODO: Calculate ∑x, ∑x2, ∑y, ∑xy, and n(number of data points).
        }

        public static void CalcReturn()
        {
            double alpha;
            double beta;

            //TODO: 

            Console.WriteLine($"The value of alphas is: {}.");
            Console.WriteLine($"The value of beta is: {}.");
            Console.WriteLine($"The formula is: {}.");
        }
    }
}



// Y = α + β(X)
// dependent variable = y-intercept + slope coefficient(independent variable)